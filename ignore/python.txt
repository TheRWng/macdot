python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

## To upgrade all at once

pip list --outdated --format=columns | tail -n +3 | awk '{print $1}' | xargs -n1 pip install -U
